language: python

sudo: required
services:
  - docker

before_install:
  - docker pull mesosphere/aws-cli
  - wget https://releases.hashicorp.com/terraform/0.11.7/terraform_0.11.7_linux_amd64.zip
  - sudo unzip terraform_0.11.7_linux_amd64.zip && mv terraform /usr/local/bin
  - terraform --version
  - aws --version

script:
  - echo "Hello world"
